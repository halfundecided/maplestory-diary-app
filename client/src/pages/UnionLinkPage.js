import * as React from 'react';
import { View, Text } from 'react-native';

const UnionLinkPage = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>UnionLink Screen</Text>
    </View>
  );
};

export default UnionLinkPage;
