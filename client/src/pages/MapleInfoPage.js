import * as React from 'react';
import { View, Text } from 'react-native';

const MapleInfoPage = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>MapleInfo Screen</Text>
    </View>
  );
};

export default MapleInfoPage;
